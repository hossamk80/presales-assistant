streamlit>=1.41.0

# Google Gen AI SDK الموحّد — يحل محل google-generativeai المهجورة
google-genai>=1.0.0

# موفّرو النص الإضافيون (المرحلة 11)
# Anthropic Claude عبر الحزمة الرسمية، والنقاط المتوافقة مع OpenAI عبر requests
anthropic>=0.40.0
requests>=2.31.0

# استخراج النصوص
pypdf>=5.0.0
docx2txt>=0.8
beautifulsoup4>=4.12.0
pandas>=2.0.0
openpyxl>=3.1.0
lxml>=4.9.0

# تحويل التواريخ الهجرية (تقويم أم القرى) — الكراسات الحكومية تؤرّخ هجرياً.
# بدونها يبقى التاريخ الهجري بلا حكم بدل قراءته ميلادياً خطأً.
hijridate>=2.4.0

# بناء المستندات
python-docx>=1.1.0

# تشفير النسخة الاحتياطية بكلمة من المستخدم (13-9).
# اختيارية عند التشغيل: بدونها تعمل النسخ والاسترجاع بلا تشفير.
cryptography>=42.0.0

# ─── اختياري: OCR للملفات الممسوحة ضوئياً ──────────────────────────────────
# يتطلب أيضاً حزم نظام:
#   sudo apt install tesseract-ocr tesseract-ocr-ara poppler-utils
# pytesseract>=0.3.10
# pdf2image>=1.17.0

# تصدير PDF (اختياري — تصدير Word لا يحتاجه)
reportlab>=4.0.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
