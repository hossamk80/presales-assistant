# حالة النظام وخطة التنفيذ

## System Status & Execution Plan

> **آخر تحديث: 2026-08-02** · النظام: `presales_codex_ready/business_analyst`
> · 324 اختباراً · CI أخضر
>
> **هذا الملف هو المرجع الوحيد** لمميزات النظام ومقارنته بالسوق ونواقصه وخطة
> إكماله. حلّ محل خمسة ملفات متفرّقة. يُحدَّث في مكانه مع كل دفعة — لا تُنشأ
> نسخة ثانية منه، ويُغيَّر تاريخ التحديث أعلاه في كل مرة.
>
> **This is the single reference** for what the system does, how it compares,
> what it lacks, and how it gets completed. It replaces five separate files
> and is updated in place — never duplicated. Update the date above each time.

---

# الجزء الأول — أين نحن

## 1. ما ينجزه النظام اليوم

الغاية المعلنة سلسلة من أربع حلقات:

> **1. تحليل الكراسة ← 2. مطابقة المتطلبات ← 3. كتابة العرض الفني ←
> 4. استخراج العرض من النظام.**

الحلقات الأربع **مكتملة وتعمل**. هذا ما تحويه:

| القدرة | الحالة | الموضع |
|---|---|---|
| رفع كل المرفقات بكل الصيغ + OCR للممسوح ضوئياً + ترجيح دور كل ملف | ✅ | `file_handler.py` · `state.py` |
| دمج المرفقات في **سياق مشروع موحّد** (الجهة · الموعد · التسليمات · القيود · الغرامات · الشهادات · المحتوى المحلي) | ✅ | `PROJECT_CONTEXT_SCHEMA` |
| **Go/No-Go** مقارَناً بقدرات الشركة، بفجوات تأهيل صريحة | ✅ | `PROMPTS["gonogo"]` |
| **مصفوفة امتثال** بعشرة حقول + **جدول كميات** بتسعة حقول، استخراج آلي وتحرير يدوي | ✅ | `state.py` · `tables.py` |
| **مستندات المظروف** مستخرَجة من الكراسة + رصد الشهادات المنتهية قبل الموعد | ✅ | `submission.py` |
| **هيكل عرض ديناميكي** وفق معايير اعتماد بسبعة أقسام إلزامية | ✅ | `ai_engine.py` |
| **كتابة قسماً بقسم** مع توجيه لكل قسم ومساعد جانبي (نقّح · اسأل) | ✅ | `doc_builder.py` |
| **لجنة مراجعة من ثلاثة وكلاء** (فني · تجاري · قانوني)، والجاهزية أضعف زاوية | ✅ | `REVIEW_LENSES` · `review.py` |
| **مصفوفة تتبّع تحجب التصدير** عند متطلب حرج بلا تغطية مؤكَّدة | ✅ متميّز | `traceability.py` |
| **تصدير Word و PDF** بغلاف وفهرس وترقيم وهوية بصرية وحقن قالب الشركة | ✅ | `file_handler.py` |
| **ذاكرة العطاءات** — نتائج منافساتك السابقة تُستدعى عند عطاء مشابه | ✅ | `history.py` |
| **مستودع معرفة** بالتضمين والاسترجاع | ⚠️ أساس فقط | `knowledge.py` |
| **ثنائية لغة كاملة** في الواجهة والمخرجات، محروسة باختبار | ✅ | `i18n.py` |

**الخلاصة**: الجوهر ليس ناقصاً. الناقص هو **الأطراف الأربعة** التي تحوّله من
أداة فرد واحد إلى منتج مؤسسي: الأدلة · الاقتصاد · الحوكمة · التحكّم.

---

## 2. مقارنة السوق

مسح أُجري في 2026-08-01 على المنصات الرائدة والمنافس المحلي. المصادر في آخر
الملف.

| المنصة | ما تُبنى عليه قيمتها | ماذا يعني لنا |
|---|---|---|
| **Loopio** (الأولى في تصنيفات 2026) | **مكتبة محتوى** أقوى ما فيها · مؤشرات نسبة الفوز وصحة المحتوى وطاقة الفريق | مكتبة المحتوى ومؤشرات الأداء **متطلب سوق أساسي** لا ترف |
| **Responsive** | تقسيم الكراسة إلى أقسام **قابلة للإسناد** · تكاملات مؤسسية · تعدد لغات | إسناد الأقسام يفترض مستخدمين وصلاحيات — غائبة عندنا تماماً |
| **Qvidian** (Upland) | قوالب مرنة تحفظ الهوية والامتثال · تقارير | حقن قالب Word عندنا يوازيها بل يتفوّق |
| **AutogenAI** | **هندسة البرومبت** والضبط الدقيق كميزة معروضة للعميل | يؤكّد أن شاشة إدارة البرومبتات ميزة تُباع لا ترف داخلي |
| **Arphie** | استشهاد بالمصدر في كل إجابة · درجة ثقة · إعلان صريح عند عدم المعرفة | فلسفتنا أصلاً — **تعادل لا فجوة** |
| **Esdaar** (سعودية، أقرب منافس) | فحص امتثال بـ 48 بنداً · **فحص محتوى محلي يتحقق من شهادة نطاقات ويحسب الدرجة** | أخطر فجوة محلية: نعالج المحتوى المحلي **نصّياً** وهم **يحسبونه رقماً** |

ثلاث خلاصات تحكم ترتيب الأولويات:

1. **ما بدا «إضافات مؤسسية» هو أساس المنافسة** — مكتبة المحتوى وإسناد الأقسام
   ومؤشرات الفوز، لا جودة التوليد.
2. **الاستشهاد وعدم التخمين لم يعودا تفوّقاً** — صارا معياراً تُسوّق به المنصات
   الحديثة. يبقيان ميزة أمام الأدوات القديمة فقط.
3. **الدرجة المحسوبة للمحتوى المحلي فجوة جديدة** يشحنها المنافس المحلي اليوم.

### فجوتان لا تُغلقان بالبرمجة

| # | الفجوة | الحالة |
|---|---|---|
| 1 | **تكامل منصة اعتماد** | ❌ محجوب — يتطلب وصولاً رسمياً. التفصيل في الملحق ب |
| 2 | **بيانات المنافسين وحركة الأسعار** | ❌ لا نملكها ولن نختلقها. ذاكرة العطاءات تغطّي الشقّ الممكن (تاريخك أنت) |

---

## 3. النواقص مرتّبة بالخطورة

### 🔴 حرجة — يُخسر بها عطاء

| # | النقص | الأثر |
|---|---|---|
| ن-1 | **لا تعامُل مع الملاحق والتعديلات والأسئلة والأجوبة** | تُبنى المصفوفة على نسخة ملغاة، والعرض يجيب متطلبات لم تعد قائمة |
| ن-2 | **لا سجل كوادر** — لا مطابقة «مدير مشروع PMP بخبرة 10 سنوات» بشخص بعينه | أثقل معيار في التقييم الفني بلا مطابقة |
| ن-3 | **لا سجل مشاريع سابقة مُهيكل** | شرط «ثلاثة مشاريع مماثلة خلال 5 سنوات» لا يُثبت آلياً |
| ن-4 | **لا سجل شهادات وتصنيفات** | Go/No-Go يقول «غير معلوم» لشهادة تملكها الشركة |
| ن-5 | **لا جدول زمني مُهيكل** — نص حر بلا مراحل ولا اعتماديات ولا معالم | قسم إلزامي (الرابع من السبعة) يخرج أضعف الأقسام |
| ن-6 | **لا مصادقة ولا مستخدمون ولا صلاحيات**، وجدول الشركة أحادي بالتصميم (`CHECK (id = 1)`) | لا يدخل النظام قسم عطاءات فيه أكثر من شخص |
| ن-7 | **لا قياس ولا خفض لاستهلاك التوكن** — ~3.4 مليون توكن للمنافسة الواحدة | الكلفة تمنع الانتشار، وغياب القياس يمنع إثبات أي تحسين |

### 🟠 عالية

| # | النقص |
|---|---|
| ن-8 | لا سجل موردين رئيسيين (OEM) ولا خطاب تفويض مصنّع (MAF) — غيابه سبب استبعاد شكلي |
| ن-9 | لا درجة محسوبة للمحتوى المحلي والسعودة |
| ن-10 | لا ملف عميل/جهة يجمع تاريخها وسلوكها التقييمي |
| ن-11 | لا مكتبة محتوى معتمد — 30–40٪ من كل عرض يُولَّد من الصفر كل مرة |
| ن-12 | لا وحدة استفسارات ولا لوحة مواعيد ولا عدّ تنازلي |
| ن-13 | لا تصدير للملاحق (سير · شهادات · سابقة أعمال) مع العرض |
| ن-14 | لا نسخ للأقسام ولا سجل تدقيق ولا سير اعتماد قبل التسليم |
| ن-15 | لا اتساق أرقام بين الأقسام (مدة في المنهجية تخالف الجدول الزمني) |
| ن-16 | الحفظ التلقائي «آخر كاتب يكسب» بلا قفل — جلستان تمحوان عمل بعضهما |
| ن-17 | لا تشفير لقاعدة البيانات ولا نسخ احتياطي من الواجهة |
| ن-18 | رفع السير الذاتية يُدخل النظام في نطاق نظام حماية البيانات الشخصية بلا سياسة احتفاظ |

### 🟡 متوسطة

| # | النقص |
|---|---|
| ن-19 | لا مسرد مصطلحات مُلزِم · لا تحقّق من تطابق النسختين في وضع «كليهما» |
| ن-20 | لا تصدير بصيغة نماذج الجهة · لا تاريخ هجري على الغلاف |
| ن-21 | لا مؤشرات أداء (نسبة فوز · زمن دورة · كلفة إعداد) ولا خطّ أنابيب |
| ن-22 | لا مخططات ولا صور في المخرَج · لا مصفوفة حل · لا حساب أحجام |
| ن-23 | لا بثّ تدريجي للمخرجات — انتظار صامت لكل قسم |
| ن-24 | استدعاء خطوط Google من الواجهة يكسر التشغيل المعزول |

---

# الجزء الثاني — خطة التنفيذ

## 4. قواعد الإنجاز — تسري على كل مهمة

لا تُقبل مهمة مهما صحّ منطقها إن خالفت واحدة من هذه:

| # | القاعدة |
|---|---|
| 1 | كل نص واجهة جديد له `ar` و `en` في `utils/i18n.py` |
| 2 | كل نص يُكتب **داخل الملف المصدَّر** يتبع `output_language` لا لغة الواجهة |
| 3 | توسيع الموجود لا استنساخه — مخطط موازٍ لمخطط قائم مرفوض |
| 4 | كل مهمة تُسلَّم باختبارات تفشل بدون التغيير، وCI أخضر |
| 5 | هذا الملف و `README.md` يُحدَّثان مع كل دفعة، بما فيه عدد الاختبارات |
| 6 | لا رقم سعري في العرض الفني — أي شاشة مالية معزولة تخزيناً وتصديراً |
| 7 | القرارات الحرجة تبقى بشرية: الالتزام · القائمة الإلزامية · الحيازة · الخوض |
| 8 | الجاهزية = أضعف زاوية لا المتوسط |

**رموز نوع العمل**: ➕ إضافة جديدة · ✏️ تعديل على قائم · 🔧 إصلاح عيب

---

## 5. المرحلة 10 — سدّ أسباب الخسارة المباشرة

**الغاية**: ألّا يُخسر عطاء بسبب النظام نفسه. **الجهد المتبقي: 15 يوماً.**

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| 10-1 | 🔧 | ✅ **منفَّذ** — إشعار السرية يتبع لغة المخرجات | `file_handler.py` · `doc_builder.py` | عرض إنجليزي بلا حرف عربي في صفحة السرية | — |
| 10-2 | 🔧 | ✅ **منفَّذ** — رمز اسم الشركة لا يحجب التصدير | `file_handler.py` · `doc_builder.py` · `review.py` | القالب يمرّ بعد ملء ملف الشركة، ويُحجب إن كان فارغاً | — |
| 10-3 | 🔧 | ✅ **منفَّذ** — قراءة التاريخ الهجري (أم القرى) | `submission.py` | شهادة تنتهي قبل موعد هجري تُرصد · هجري غير قابل للتحويل يبقى بلا حكم | — |
| 10-4 | 🔧 | ✅ **منفَّذ** — إعادة المحاولة على الفشل العابر | `ai_engine.py` | 503 يُعاد · 401 لا يُعاد · المستخدم يُبلَّغ | — |
| 10-5 | 🔧 | ✅ **منفَّذ** — رسائل `utils/` من `i18n` وحارسها في CI | `ai_engine.py` · `knowledge.py` · `i18n.py` | الحارس يرصد العربية داخل f-string | — |
| 10-6 | 🔧 | ✅ **منفَّذ** — تشغيل Codespaces يعمل في أي وقت | `.devcontainer/` · `scripts/` | `./scripts/run.sh` و `test.sh` يعملان من أي مجلد | — |
| **10-7** | ➕ | **الملاحق والتعديلات**: إصدار لكل رفعة · مقارنة نصية · وسم صفوف المصفوفة المتأثرة «تحتاج مراجعة» | `db.py` (جدول `attachment_versions`) · `analysis.py` | رفع نسخة ثانية يُظهر ما تغيّر ويُعلّم المتأثر | 4 ي |
| **10-8** | ✏️ | **الجدول الزمني المُهيكل**: `TIMELINE_SCHEMA` (مرحلة · بداية · مدة · اعتمادية · تسليم · معلم دفع · وزن) بدل النص الحر | `ai_engine.py` · `state.py` · `tables.py` | استخراج يملأ الجدول ويبقى قابلاً للتحرير كجدول الكميات | 3 ي |
| **10-9** | ➕ | **مخطط Gantt في المخرَج** صورةً في Word و PDF | `file_handler.py` | يظهر في الصيغتين باتجاه الكتابة الصحيح | 2 ي |
| **10-10** | ➕ | **تحقّق آلي من الجدول**: المدد ≤ مدة العقد · لا معلم بعد الموعد · كل تسليم له معلم | `utils/timeline.py` | تجاوز مدة العقد يُحذَّر منه قبل التصدير | 2 ي |
| **10-11** | ➕ | **اتساق الأرقام بين الأقسام** (مدد · نسب · أعداد) | `utils/consistency.py` · `review.py` | تناقض مدة بين قسمين يظهر ملاحظة في المراجعة | 2 ي |
| **10-12** | ➕ | **لوحة المواعيد**: عدّ تنازلي · صلاحية الضمان الابتدائي · سريان العرض | `projects.py` · `submission.py` | الموعد المقروء يظهر عدّاً تنازلياً في كل شاشة | 2 ي |

---

## 6. المرحلة 11 — طبقة الذكاء الاصطناعي: الموفّرون والاقتصاد

**الغاية**: خيارات واسعة من الأدوات والنماذج، وكلفة تسمح بالتبنّي، وقياس يُثبت
التحسين. **الجهد: 24 يوماً.**

> **لماذا قبل طبقة الأدلة**: السجلات المُهيكلة تزيد حجم السياق المُمرَّر
> للنموذج. بناؤها فوق اقتصاد توكن غير مُصلَح يضاعف الكلفة ويجعل قياس أثرها
> مستحيلاً.

### 6.1 تعدد الموفّرين والنماذج

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| **11-1** | ➕ | **طبقة تجريد الموفّرين** `providers/`: واجهة واحدة (`generate` · `generate_json` · `embed` · `count_tokens` · `usage`) ينفّذها كل موفّر | `utils/providers/` | تبديل الموفّر لا يمسّ أي ملف في `views/` | 4 ي |
| **11-2** | ➕ | **موفّرو النص**: Google Gemini (قائم) · OpenAI · Anthropic Claude · أي نقطة **متوافقة مع OpenAI** (OpenRouter · Azure OpenAI · Groq · Mistral · DeepSeek · Together) · **محلي** (Ollama / vLLM) | `utils/providers/` | كل موفّر يعمل بمفتاحه، والمحلي يعمل بلا مفتاح | 5 ي |
| **11-3** | ➕ | **سجل النماذج**: لكل موفّر قائمة نماذجه مع نافذة السياق ودعم JSON المُهيكل والسعر لكل مليون توكن، قابلة للتحديث بلا تعديل شيفرة | `utils/providers/catalog.py` | إضافة نموذج جديد بتحرير السجل وحده | 2 ي |
| **11-4** | ✏️ | **شاشة الإعدادات الموسّعة**: اختيار الموفّر ← النموذج ← الحرارة وحد المخرَج · اختبار اتصال يعرض الزمن والكلفة · حذف الحقول المعطّلة | `views/settings.py` | لا حقل معطّل ظاهر، وكل خيار معروض يعمل فعلاً | 3 ي |
| **11-5** | ➕ | **نموذج لكل مهمة**: الأخف للاستخراج والتصنيف · الأقوى للهيكل والمراجعة · قابل للتجاوز يدوياً | `providers/` · `settings.py` | لكل مهمة نموذج افتراضي معلن | 1 ي |
| **11-6** | ✏️ | **موفّر التضمين منفصل عن موفّر النص** — مع **تحذير إلزامي**: تغيير نموذج التضمين يُبطل متجهات المستودع المخزَّنة (`kb_chunks.dims`)، فتُهمَل المقاطع القديمة صامتةً. يلزم زر **إعادة فهرسة** وعرض عدد المقاطع المعطَّلة | `knowledge.py` · `db.py` · `company.py` | تغيير النموذج يعرض التحذير ولا يُفقد المستودع بلا علم | 2 ي |

### 6.2 القياس والخفض

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| **11-7** | ➕ | **جدول `ai_usage`**: المنافسة · المهمة · الموفّر · النموذج · توكن الإدخال · المخزَّن منه · المخرَج · الكلفة · الزمن · النتيجة | `db.py` · `providers/` | كل استدعاء يُسجَّل من عدّاد الموفّر لا من تقدير محلي | 2 ي |
| **11-8** | ➕ | **شاشة الاستهلاك**: إجمالي لكل منافسة · توزيع على الوكلاء · مقارنة النماذج والموفّرين | `views/settings.py` | الرقم يطابق فاتورة المزوّد بهامش معقول | 2 ي |
| **11-9** | ➕ | **حدّ إنفاق شهري**: إنذار عند 80٪ وإيقاف عند 100٪ | `settings.py` · `providers/` | التجاوز يمنع الاستدعاء برسالة صريحة لا بفشل صامت | 1 ي |
| **11-10** | ✏️ | **الموجز المضغوط بدل الخام**: الكتابة تستند إلى السياق الموحّد والمصفوفة لا لنص الكراسة الكامل | `state.py` · `doc_builder.py` | توكن كتابة القسم ينخفض ≥ 85٪ بلا تراجع في الجودة | 2 ي |
| **11-11** | ➕ | **فهرسة الكراسة واسترجاعها**: تقسيم وتضمين الكراسة كمستودع المعرفة، وتمرير البنود ذات الصلة بالقسم | `knowledge.py` · `analysis.py` | متطلب في صفحة 180 يُسترجَع للقسم الذي يخصّه | 4 ي |
| **11-12** | ➕ | **التخزين المؤقت للسياق** حيث يدعمه الموفّر | `providers/` | عدّاد التوكن المخزَّن > 0 في السجل | 3 ي |
| **11-13** | ➕ | **تخزين نتائج الاستدعاءات**: بصمة (تعليمات + سياق + نموذج) ← النتيجة | `db.py` · `providers/` | إعادة الضغط بلا تغيير معطيات لا تُنفق توكن | 2 ي |

**مقياس نجاح المرحلة**: كلفة المنافسة الواحدة قبل وبعد — الهدف انخفاض 15–25 ضعفاً.

---

## 7. المرحلة 12 — طبقة الأدلة

**الغاية**: مطابقة مبنية على **سجلات** لا على نص حر. **الجهد: 26 يوماً.**

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| **12-1** | ➕ | **سجل الكوادر** `people`: الاسم · الدور · سنوات الخبرة · الشهادات وصلاحيتها · اللغات · الإتاحة · ملف السيرة | `db.py` · `company.py` | إضافة شخص وربط سيرته، وبقاؤه بعد إغلاق الجلسة | 3 ي |
| **12-2** | ➕ | **مصفوفة الكوادر الرئيسية**: دور تشترطه الكراسة ← المرشّح ← دليل المطابقة ← الفجوة | `ai_engine.py` · `tables.py` | كل دور في الكراسة له صف، والدور بلا مرشّح يظهر فجوة | 3 ي |
| **12-3** | ➕ | **سجل سابقة الأعمال** `references`: العميل · القطاع · النطاق · نطاق القيمة · المدة · دورنا · شهادة الإنجاز · جهة مرجعية | `db.py` · `company.py` | «ثلاثة مشاريع مماثلة» يُطابَق بصفوف لا بنص | 3 ي |
| **12-4** | ➕ | **سجل الشهادات والتصنيفات** `certificates`: النوع · الرقم · الجهة · الإصدار · الانتهاء · الملف | `db.py` · `company.py` | شهادة تنتهي قبل الموعد تُرصد من السجل لا بإدخال يدوي | 2 ي |
| **12-5** | ➕ | **سجل الموردين الرئيسيين** `vendors`: المورّد · خط المنتجات · مستوى الشراكة · خطاب التفويض وصلاحيته · الدعم المحلي · EOL/EOS · البديل | `db.py` · `company.py` | خطاب تفويض منتهٍ أو غائب يظهر في مستندات المظروف | 3 ي |
| **12-6** | ✏️ | **حقن السجلات في Go/No-Go** بدل نبذة الشركة وحدها | `state.py` · `ai_engine.py` | متطلب يقابله صف يخرج «نعم + المستند»، وبلا صف «غير معلوم» | 2 ي |
| **12-7** | ➕ | **ملف الجهة/العميل** `entities`: القطاع · جهات الاتصال · تاريخ منافساتنا · أنماط تقييمها · متطلباتها المتكررة | `db.py` · `projects.py` · `history.py` | فتح منافسة لجهة معروفة يستدعي ملفها تلقائياً | 3 ي |
| **12-8** | ➕ | **درجة المحتوى المحلي والسعودة**: نسبة نطاقات · درجة تقديرية · الفجوة عن الحد المطلوب في الكراسة | `utils/local_content.py` · `tables.py` | رقم صريح لا التزام نصّي، **معروض تقديراً يراجعه بشر** | 4 ي |
| **12-9** | ➕ | **تصدير الملاحق**: السير والشهادات وسابقة الأعمال كملاحق مرقّمة مع العرض | `file_handler.py` · `doc_builder.py` | ملف واحد بترقيم متصل وفهرس يشمل الملاحق | 3 ي |

---

## 8. المرحلة 13 — الاستخدام الجماعي

**الغاية**: أن يدخل النظام قسم عطاءات فيه أكثر من شخص. **الجهد: 26 يوماً.**

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| **13-1** | ✏️ | **إلغاء قيد الشركة الأحادية** `CHECK (id = 1)` + ترحيل قاعدة قائمة | `db.py` | قاعدة قديمة تُرقّى بلا فقد بيانات | 2 ي |
| **13-2** | ➕ | **مستخدمون ومصادقة**: `users` بتجزئة قوية أو دخول موحّد | `db.py` · `settings.py` · `app.py` | لا وصول لأي شاشة قبل الدخول | 4 ي |
| **13-3** | ➕ | **خمسة أدوار**: مدير نظام · مدير عطاءات · كاتب · مراجع · مطّلع | `utils/auth.py` · كل `views/` | الكاتب لا يرى المفاتيح ولا يحذف منافسة | 3 ي |
| **13-4** | ➕ | **إسناد الأقسام**: لكل قسم مالك، ولوحة تُظهر حالة كل قسم ومالكه | `state.py` · `doc_builder.py` | تعديل قسم لا يملكه المستخدم محجوب | 2 ي |
| **13-5** | ➕ | **سجل تدقيق**: من غيّر ماذا ومتى، وأي فقرة مصدرها النموذج | `db.py` · `utils/audit.py` | كل تغيير قابل للتتبّع لشخص ووقت | 3 ي |
| **13-6** | ➕ | **نسخ الأقسام واسترجاعها** مع مقارنة | `db.py` · `doc_builder.py` | استرجاع نسخة قبل ثلاثة تعديلات | 3 ي |
| **13-7** | 🔧 | **حلّ تعارض الحفظ التلقائي**: قفل أو دمج بدل «آخر كاتب يكسب» | `projects.py` · `db.py` | جلستان على منافسة واحدة لا تمحوان عمل بعضهما | 2 ي |
| **13-8** | ➕ | **سير الاعتماد قبل التسليم**: مدير العطاءات ← المالية ← الاعتماد النهائي | `review.py` · `db.py` | لا تصدير نهائي بلا اعتماد مسجَّل | 2 ي |
| **13-9** | ➕ | **تشفير قاعدة البيانات + نسخ احتياطي واسترجاع من الواجهة** | `db.py` · `settings.py` | نسخة كاملة تشمل المعرفة وملف الشركة، تُستعاد بنجاح | 3 ي |
| **13-10** | ➕ | **سياسة البيانات الشخصية**: أساس معالجة السير · مدة احتفاظ · حذف عند الطلب | `db.py` · `company.py` | حذف شخص يحذف سيرته ومقاطعها من المستودع | 2 ي |

---

## 9. المرحلة 14 — التحكّم والمحتوى والتوسّع

**الغاية**: نظام يُضبط لكل قطاع وكل عميل. **الجهد: 32 يوماً.**

| المعرّف | النوع | المهمة | الملفات | شرط القبول | الجهد |
|---|---|---|---|---|---|
| **14-1** | ✏️ | **إدارة البرومبتات**: `prompts` (مفتاح · وكيل · قطاع · لغة · إصدار · نص · مفعّل) مزروع من ثوابت الشيفرة | `db.py` · `ai_engine.py` | تعديل يسري بلا إعادة تشغيل، والافتراضي يُستعاد بنقرة | 4 ي |
| **14-2** | ➕ | **القواعد الثابتة غير القابلة للتحرير** تُلحق بكل برومبت — **شرط قبول لـ 14-1 لا مهمة تالية له** | `ai_engine.py` | تعديل برومبت لا يُزيل منع التسعير ولا شرط القرار البشري | 1 ي |
| **14-3** | ➕ | **تجربة برومبت قبل الاعتماد** على منافسة حقيقية بمقارنة جنباً إلى جنب | `settings.py` | مقارنة النتيجتين قبل التفعيل | 2 ي |
| **14-4** | ➕ | **مكتبة المحتوى المعتمد**: كتل قابلة لإعادة الاستخدام بحالة اعتماد وتاريخ مراجعة | `db.py` · `company.py` · `doc_builder.py` | إدراج كتلة معتمدة في قسم بلا استدعاء نموذج | 4 ي |
| **14-5** | ➕ | **بصمة الأسلوب من عرض سابق**: فئة `proposal_sample` + استخراج نبرة وبنية تُحقن في التوليد | `knowledge.py` · `ai_engine.py` | عرضان لنفس الشركة بنبرة واحدة | 3 ي |
| **14-6** | ➕ | **مسرد المصطلحات** يوحّد الترجمة الفنية عبر الأقسام | `db.py` · `ai_engine.py` | «SLA» بصيغة واحدة في كل العرض | 2 ي |
| **14-7** | ➕ | **مؤشرات الأداء**: نسبة الفوز حسب الجهة والقطاع · زمن الدورة · كلفة الإعداد · إعادة الاستخدام | لوحة · `history.py` · `db.py` | لوحة البداية تعرض قياساً لا تعريفاً | 3 ي |
| **14-8** | ➕ | **خطّ الأنابيب واستراتيجية العرض**: مرحلة · مالك · احتمال الفوز · «لماذا نفوز» تُحقن في كل قسم | `projects.py` · `state.py` | حقل الاستراتيجية يظهر أثره في نص الأقسام | 3 ي |
| **14-9** | ➕ | **وحدة الاستفسارات**: أسئلة للجهة بمواعيدها وأجوبتها، وربطها بصفوف المصفوفة | `db.py` · `tables.py` | غموض مرصود يصير سؤالاً مُتتبَّعاً | 2 ي |
| **14-10** | ➕ | **موصّل أوديو** خلف `connectors/` مجرّدة، تفعيل لكل منافسة مع تحذير خروج البيانات | `utils/connectors/` | سحب العملاء والمنتجات والموظفين · دفع الفائزة كأمر بيع | 6 ي |
| **14-11** | ➕ | **تحويل الفائز إلى مشروع** بالتزاماته المستخرجة من العرض | `db.py` · `projects.py` | التزامات العرض تصير قائمة تسليمات | 3 ي |

---

## 10. الملخص والتسلسل

| المرحلة | الغاية | الجهد | لماذا هذا الترتيب |
|---|---|---|---|
| **10** | ألّا يُخسر عطاء بسبب النظام | 15 ي | خسارة عطاء أغلى من أي ميزة مؤجَّلة |
| **11** | خيارات ذكاء اصطناعي واسعة + كلفة تسمح بالتبنّي | 24 ي | كل ما بعده يمرّ من هذه الطبقة، وبناؤه فوق اقتصاد مكسور يضاعف الكلفة |
| **12** | مطابقة مبنية على سجلات | 26 ي | بلا أدلة مُهيكلة تبقى المطابقة نصية لا تصمد أمام لجنة فحص |
| **13** | فريق لا فرد | 26 ي | المنافسون يشحنون إسناد الأقسام؛ بلا مستخدمين لا يدخل قسم عطاءات |
| **14** | ضبط وتوسّع | 32 ي | يبني على ما قبله ولا يسبقه |
| | **الإجمالي** | **123 يوماً** | |

### أسرع مسار لأثر ملموس

لو توفّر أسبوعان: **10-7** (الملاحق والتعديلات) و**10-8** (الجدول الزمني)
و**11-7** (قياس الاستهلاك). الأول يمنع بناء عرض على كراسة ملغاة، والثاني يقوّي
أضعف قسم إلزامي، والثالث يجعل كل تحسين لاحق قابلاً للإثبات.

---

## 11. المخاطر على الخطة نفسها

| الخطر | الأثر | التخفيف |
|---|---|---|
| **13-2 (المصادقة) يمسّ كل شاشة** | ارتداد واسع في `views/` | يُنفَّذ بعد استقرار 10 و 12، وبطبقة `utils/auth.py` واحدة |
| **11-11 (استرجاع الكراسة) قد يُسقط متطلباً** | متطلب لا يصل للنموذج فلا يُغطّى | الاستخراج الأول يبقى على النص الكامل؛ الاسترجاع للكتابة وحدها، ومصفوفة التتبّع تبقى الحارس |
| **11-6 (تبديل نموذج التضمين)** | المستودع يُهمَل صامتاً | تحذير إلزامي + عدّ المقاطع المعطَّلة + زر إعادة فهرسة |
| **14-1 (تحرير البرومبتات)** | تعديل واحد يزيل منع التسعير | 14-2 شرط قبول لا مهمة تالية |
| **12-8 (درجة المحتوى المحلي)** | رقم خاطئ له كلفة نظامية | يُعرض تقديراً صريحاً يراجعه بشر، ومرجعه يُرفع للمستودع |
| **تقديرات الجهد** | انزلاق الجدول | لمطوّر واحد ملمّ بالنظام؛ تُراجَع بعد المرحلة 10 |

---

# الملاحق

## ملحق أ — النشر على Replit

### نوع النشر يحدّد بقاء بياناتك

كل عمل النظام يعيش في ملف SQLite واحد على القرص:

| نوع النشر | القرص | مناسب؟ |
|---|---|---|
| **Reserved VM** | دائم | ✅ الخيار الصحيح |
| Autoscale | يُمسح عند كل إعادة تشغيل | ❌ ستفقد كل المنافسات |
| Static | لا يشغّل Python | ❌ |

`.replit` مضبوط على `deploymentTarget = "vm"`. لا تغيّره إلى `autoscale` إلا
بعد نقل التخزين إلى قاعدة خارجية. (وضع التطوير داخل الـ Repl قرصه دائم كذلك،
فالخطر محصور في نوع النشر.)

### الخطوات

1. ادفع المستودع إلى GitHub، ثم **Create Repl → Import from GitHub**.
2. ضع المفتاح في **Secrets** باسم `GEMINI_API_KEY` — لا في الكود ولا في `.replit`.
3. `pip install -r presales_codex_ready/business_analyst/requirements.txt`
   وللـ OCR: `pip install pytesseract pdf2image`.
4. اضغط **Run**. الربط بـ `0.0.0.0` ضروري؛ `127.0.0.1` يجعل التطبيق غير مرئي.

`replit.nix` يتكفّل بحزم النظام: `tesseract` و `poppler_utils` للـ OCR،
و`dejavu_fonts` لتصدير PDF العربي. إن لم يجد tesseract العربية بدّله إلى
`pkgs.tesseract4`.

### قاعدة البيانات والنسخ الاحتياطي

`ANALYST_DB_PATH = "/home/runner/${REPL_SLUG}/data/analyst.db"` — خارج شجرة
الكود لئلا يُمسح مع كل مزامنة. الملف مُستثنى من git عمداً؛ انسخه دورياً:

```bash
cp "$ANALYST_DB_PATH" "backup-$(date +%F).db"
```

### ⚠️ الخصوصية تتغيّر بالنشر

النظام صُمّم للتشغيل المحلي: بيانات العطاءات لا تغادر الجهاز إلا في استدعاءات
النموذج. **النشر على خادم خارجي يغيّر هذا** — الكراسات وملف الشركة والمستودع
تصير على خوادم طرف ثالث. تأكّد أن الـ Repl **خاص**، وراجع ما إذا كانت سياسة
شركتك أو شروط الجهة تسمح بذلك. للبيانات الحسّاسة يبقى التشغيل المحلي الأسلم.

### حصر الأعطال الشائعة

| العَرَض | السبب المرجّح |
|---|---|
| صفحة بيضاء أو "connecting…" لا تنتهي | استماع على `127.0.0.1` بدل `0.0.0.0`، أو `enableCORS` مفعّل |
| نص PDF مشوّه أو رفض التصدير | لا خط عربي — تأكد من `dejavu_fonts` |
| خط Word ليس المختار | الخط غير مثبَّت على جهاز من يفتح الملف؛ الملف لا يحمله بداخله |
| كراسة ممسوحة تعطي تحليلاً فارغاً | OCR غير مثبّت |
| اختفاء المنافسات بعد النشر | النشر Autoscale — انقل إلى Reserved VM |
| «يرجى إدخال مفتاح Gemini» | `GEMINI_API_KEY` غير مضبوط في Secrets |

---

## ملحق ب — تكامل منصة اعتماد

**الحالة: غير منفَّذ — محجوب بوصول لا يملكه المشروع.**

### لماذا

اعتماد لا تنشر واجهة برمجية عامة للمورّدين تتيح تصفّح المنافسات أو تنزيل
الكراسات برمجياً، وأي وصول يمرّ عبر تفويض رسمي مرتبط بهوية المنشأة.

**الكشط الآلي بانتحال جلسة المستخدم مرفوض هندسياً** — لا لصعوبته بل لأنه
يخالف شروط المنصة ويعرّض حساب المنشأة للإيقاف، ويتطلب تخزين بيانات دخول
حكومية داخل التطبيق، وهشّ يكسر صامتاً فيُبنى على بياناته الناقصة قرار خوض خاطئ.

### ما يلزم لو توفّر الوصول

العمل محدود لأن ما بعد الاستيراد جاهز: يكفي دالة تُرجع ما يُرجعه الرفع اليدوي
(`{"name", "entity", "reference", "rfp_raw_text", "attachments"}`) ثم تُمرَّر
إلى `db.create_project()`. كل ما بعدها — الدمج والاستخراج والهيكل والكتابة
والمراجعة والتصدير — يعمل بلا تعديل.

### البديل العملي اليوم

الفارق الفعلي **خطوة تنزيل واحدة لكل منافسة**: نزّل المرفقات من اعتماد، أنشئ
منافسة، ارفع الملفات دفعة واحدة (النظام يقرأ كل الصيغ ويشغّل OCR ويرجّح دور كل
ملف)، ثم **دمج المرفقات**.

---

## المصادر / Sources

- [Best RFP Response Management Software 2026 — Loopio](https://loopio.com/blog/best-response-management-software/)
- [RFP Software Comparison 2026 — Knowlee](https://www.knowlee.ai/blog/rfp-software-comparison)
- [Best AI Proposal Software 2026 — AutogenAI](https://autogenai.com/blog/best-ai-proposal-software-2026/)
- [Arphie Pricing Guide 2026 — AutoRFP.ai](https://autorfp.ai/blog/arphie-pricing)
- [Esdaar — AI Tender Platform for Saudi Contractors](https://esdaar.com/en)
- [LCGPA Local Content in Saudi Tenders — Saudi Compliance Institute](https://saudicomplianceinstitute.com/en/blogs/news/lcgpa-local-content-certificate-saudi-tenders)
- [Etimad Government Procurement Portal 2026 — IncorpMENA](https://incorpmena.com/saudi-etimad-procurement)
